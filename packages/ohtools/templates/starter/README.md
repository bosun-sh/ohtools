# __APP_NAME__

Ohtools app scaffolded with `@bosun-sh/ohtools`.

```sh
bun install
bun run ohtools:list
bun run ohtools:hello
```

Agents can discover local Ohtools guidance in `.agents/skills/ohtools/`.
