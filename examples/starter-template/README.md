# Ohtools Starter Template

Bun and TypeScript starter for an Ohtools app.

```sh
bun install
bun run dev
bun run typecheck
```

Generate local tool docs from the workspace package:

```sh
bunx ohtools --app ./src/app.ts docs
bunx ohtools --app ./src/app.ts docs --format json
```
