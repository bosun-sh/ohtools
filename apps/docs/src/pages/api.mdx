---
layout: ../layouts.astro
title: API
---

# API

Public exports:

- `Ohtools`, `plugin`, `GroupBuilder`, `PluginBuilder`
- core types and errors
- `schema`, `jsonSchema`, `InferSchema`
- `mcpAdapter` from `ohtools/adapters/mcp`
- `cliAdapter` from `ohtools/adapters/cli`
