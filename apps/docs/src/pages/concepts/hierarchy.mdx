---
layout: ../../layouts.astro
title: Hierarchy Concept
---

# Hierarchy

Groups are organizational nodes. Tools and groups form a graph with `contains`
and `next` edges. Cycles are allowed as explicit graph edges and traversal keeps
paths bounded.
