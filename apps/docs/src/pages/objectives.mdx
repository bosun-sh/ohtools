---
layout: ../layouts.astro
title: Objectives
description: Understand the Ohtools launch harness, OKRs, KPIs, stage gates, and validation commands used for release readiness.
section: Reference
order: 3
---

# Objectives

Ohtools uses project context docs and validation scripts as the launch harness.
This page points to the local sources that define readiness.

When to use this: use it before a release, compatibility change, or docs update
that needs more than a narrow smoke test.

```sh
bun run validate:stage0
bun run validate:stage5
bun run release:check
```

## Harness Documents

- OKRs: `docs/OKRS.md`
- KPIs: `docs/KPIS.md`
- Definition of ready and done: `docs/TASK-HARNESS.md`
- Validation scripts and stage gates: `docs/VALIDATION.md`

## Stage Gates

Stage gates are exposed as `validate:stage0` through `validate:stage5`, with
`release:check` running the release-blocking local checks.

Docs-facing changes should usually include:

- `bun run docs:build`
- `bun run docs:links`
- `bun run docs:snippets`
- `bun run format:check` when layout or prose changed broadly

Use [Changelog](/changelog) to record compatibility notes.
