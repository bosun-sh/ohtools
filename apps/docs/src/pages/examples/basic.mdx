---
layout: ../../layouts.astro
title: Basic Example
---

# Basic Example

The runnable source lives in `examples/basic`. It defines a plugin, composes an
app, explores a tool, runs it, and registers MCP exposure.
